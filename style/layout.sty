\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{style/layout}[2026/05/12 v2.0 CV layout]

\RequirePackage[
  a4paper,
  left   = 1.5cm,
  right  = 1.5cm,
  top    = 1.1cm,
  bottom = 1.3cm,
]{geometry}

\RequirePackage{tabularx}
\RequirePackage{array}
\RequirePackage{enumitem}
\RequirePackage{tikz}
\RequirePackage{graphicx}
\RequirePackage{fancyhdr}

\RequirePackage{hyperref}
\hypersetup{
  colorlinks = true,
  urlcolor   = cvink,
  linkcolor  = cvink,
  citecolor  = cvink,
  pdftitle   = {Fredrik Carsten Hansteen — CV},
  pdfauthor  = {Fredrik Carsten Hansteen},
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\linespread{1.05}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyfoot[R]{\small\color{cvmuted}\thepage}

\setlist{nosep}
\setlist[itemize]{
  label       = {\textbullet},
  leftmargin  = 1.1em,
  itemsep     = 1pt,
  topsep      = 1pt,
  parsep      = 0pt,
}

\endinput
