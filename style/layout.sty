\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{style/layout}[2026/05/12 v2.1 CV layout]

\RequirePackage[
  a4paper,
  left   = 1.5cm,
  right  = 1.5cm,
  top    = 1.1cm,
  bottom = 1.3cm,
]{geometry}

\RequirePackage{tabularx}
\RequirePackage{array}
\RequirePackage{enumitem}
\RequirePackage{tikz}
\RequirePackage{graphicx}
\RequirePackage{fancyhdr}
\RequirePackage{needspace}
\RequirePackage{fontawesome5}

\RequirePackage{hyperref}
\hypersetup{
  colorlinks  = true,
  urlcolor    = cvink,
  linkcolor   = cvink,
  citecolor   = cvink,
  bookmarks   = true,
  pdftitle    = {CV - Fredrik Carsten Hansteen},
  pdfauthor   = {Fredrik Carsten Hansteen},
  pdfsubject  = {Software Engineer},
  pdfkeywords = {software engineering, TypeScript, JavaScript, Python, Java, Kotlin, Dart,
                 React, Next.js, Flutter, Spring, Docker, Git, REST API, PostgreSQL,
                 Google Cloud Platform, Firebase, CI/CD},
}

\RequirePackage{lastpage}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\linespread{1.05}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\setlist{nosep}
\setlist[itemize]{
  label       = {\cvsquarebullet},
  leftmargin  = 1.8em,
  rightmargin = 4.0cm,
  itemsep     = \cvspacebullet,
  topsep      = \cvspacebullet,
  parsep      = 0pt,
}

\endinput
