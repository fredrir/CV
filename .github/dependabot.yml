version: 2
updates:
  # Keep the versions of actions used in workflows + the composite action patched.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns: ['*']
